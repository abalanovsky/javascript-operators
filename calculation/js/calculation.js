const firstOperand = Number(prompt('Enter first operand'))
const secondOperand = Number(prompt('Enter second operand'))

alert(`Results are:\n 
    ${firstOperand} + ${secondOperand} = ${firstOperand + secondOperand}
    ${firstOperand} - ${secondOperand} = ${firstOperand - secondOperand} 
    ${firstOperand} * ${secondOperand} = ${firstOperand * secondOperand} 
    ${firstOperand} / ${secondOperand} = ${firstOperand / secondOperand}
`)
